#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Executa validação mais completa antes de fazer o push
echo "🔍 Executando validação completa antes do push..."
npm run validate && npm run lint && npm run test:unit
